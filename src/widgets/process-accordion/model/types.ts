// Re-exported for convenience — canonical definitions live in
// entities/project (widgets may depend on entities, not the other way
// around, and this data is genuinely project content).
export type { ProcessStep, ProcessBlock } from '@entities/project';
