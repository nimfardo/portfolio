// Re-exported for convenience — canonical definition lives in
// entities/project (widgets may depend on entities, not the other way
// around, and this data is genuinely project content).
export type { GalleryRow } from '@entities/project';
