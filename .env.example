# Unlocks the NDA-gated project pages (client-side gate — see
# src/shared/lib/password-gate.ts; this is obscurity, not real security).
# Generate the hash from your actual password:
#   printf '%s' 'your-password' | shasum -a 256 | cut -d' ' -f1
PUBLIC_NDA_PASSWORD_HASH=
